<?php
namespace Config;

class Stripe
{
    // Secret key (used for server-side API calls)
    public static $secretKey = '';

    // Publishable key (used client-side in JS)
    public static $publishableKey = '';
}
